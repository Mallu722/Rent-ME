{
  "name": "rent-me",
  "version": "1.0.0",
  "description": "Full-stack companion booking application",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd mobile && npm start",
    "server:build": "cd server && npm run build",
    "client:build": "cd mobile && npm run build"
  },
  "keywords": [
    "companion",
    "booking",
    "social"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "mongoose": "^9.1.5"
  }
}
